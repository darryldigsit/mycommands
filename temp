# One-time setup (run these sequentially)
sudo apt update && sudo apt upgrade -y
sudo apt install -y python3 python3-pip python3-venv python3-dev build-essential libssl-dev libffi-dev git
sudo mkdir -p /opt/caldera && sudo chown $USER:$USER /opt/caldera
cd /opt/caldera
sudo git clone https://github.com/mitre/caldera.git --recursive .
sudo git checkout 5.3.0
sudo git submodule update --init --recursive
sudo chown -R $USER:$USER /opt/caldera
python3 -m venv caldera_env
source caldera_env/bin/activate
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt

# Subsequent starts (from /opt/caldera directory)
source caldera_env/bin/activate
python3 server.py

----------------------------------

sudo nano /etc/apt/sources.list

deb http://http.kali.org/kali kali-rolling main contrib non-free non-free-firmware
deb-src http://http.kali.org/kali kali-rolling main contrib non-free non-free-firmware

sudo apt update

sudo apt install -y --fix-missing

sudo apt install -y python3 python3-pip python3-venv python3-dev

# Group 1: Python basics
sudo apt install -y python3 python3-pip python3-venv

# Group 2: Development tools
sudo apt install -y python3-dev build-essential

# Group 3: Security libraries
sudo apt install -y libssl-dev libffi-dev

# Group 4: Version control
sudo apt install -y git

sudo nano /etc/apt/sources.list

deb https://kali.download/kali kali-rolling main contrib non-free non-free-firmware
deb-src https://kali.download/kali kali-rolling main contrib non-free non-free-firmware

deb http://ftp.cc.uoc.gr/mirrors/linux/kali kali-rolling main contrib non-free non-free-firmware
deb-src http://ftp.cc.uoc.gr/mirrors/linux/kali kali-rolling main contrib non-free non-free-firmware

sudo apt update
sudo apt install -y python3 python3-pip python3-venv python3-dev build-essential libssl-dev libffi-dev git

ping -c 4 8.8.8.8
ping -c 4 google.com

curl -I http://http.kali.org/kali
# or
wget --spider https://kali.download/kali/

nslookup http.kali.org
# or
dig http.kali.org

sudo apt clean && sudo rm -rf /var/lib/apt/lists/* && sudo mkdir -p /var/lib/apt/lists/partial && sudo apt update && sudo apt install -y --fix-missing python3 python3-pip python3-venv python3-dev build-essential libssl-dev libffi-dev git

python3 --version
pip3 --version
git --version
gcc --version
-----------------------------------------------------------------------------

sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 827C8569F2518CC677FECAIAED65462EC8D5E4C5 
wget -q -O - https://archive-4.kali.org/archive-key.asc | sudo apt-key add -
curl -fsSL https://archive-4.kali.org/archive-key.asc | sudo gpg --dearmor -o /usr/share/keyrings/kali-archive-keyring.gpg
sudo nano /etc/apt/sources.list
    deb [signed-by=/usr/share/keyrings/kali-archive-keyring.gpg] https://kali.download/kali kali-rolling main contrib non-free non-free-firmware
    deb-src [signed-by=/usr/share/keyrings/kali-archive-keyring.gpg] https://kali.download/kali kali-rolling main contrib non-free non-free-firmware
sudo apt update
sudo apt install -y python3 python3-pip python3-dev
python3 -m venv /opt/caldera/caldera_env
